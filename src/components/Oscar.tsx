type OscarProps={
    children:React.ReactNode
}
export const Oscor=(props:OscarProps)=>{
    return (
        <div>
            {/* Oscar Goes to P.T.
             */}
             {props.children}<hr/>
        </div>
    )
}